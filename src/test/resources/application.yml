spring:
  datasource:
    url: jdbc:h2:mem:testdb;MODE=MySQL # MySQL 모드 사용
    driver-class-name: org.h2.Driver
    username:
    password:

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: create-drop
    show-sql: true